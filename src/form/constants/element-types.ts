export const availableTypes = [
  'input',
  'select',
  'radio',
  'checkbox',
  'textarea',
  'file',
  'date',
  'container',
] as const;

export const availablePositions = ['center', 'left', 'right'] as const;
